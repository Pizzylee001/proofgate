.env
.env.*
out/
cache/
broadcast/
node_modules/
lib/
.venv-deck/
chrome-install.log

# allow placeholder example (no secrets)
!web/.env.example

# web app shared server code (root lib/ rule is for forge deps)
!web/lib/
